export const viewport = {
  width: "device-width",
  initialScale: 0.8,
};

export const metadata = {
  title: "QBills | Login",
  description: "",
};

const LoginLayout = ({ children }) => {
  return children;
};

export default LoginLayout;
